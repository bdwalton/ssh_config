HOST *.biota *.biota.utoronto.ca
     PORT 2222

HOST cswbo
  HOSTNAME login.bo.opencsw.org

HOST cswgo
  HOSTNAME build8s.go.opencsw.org

HOST o1 o1.chass o1.chass.utoronto.ca
  HOSTNAME 128.100.160.2

HOST acct*
  HOSTNAME acct.cquest.utoronto.ca
  IdentityFile ~/.ssh/id_dsa_aux

HOST ss-081* ss-079* ss-025 ss-026 ss-027 ss-028 ss-029 ss-03*
  USER bwalton

#these are all windows boxes.  we typically want to hit them as rootadmin
HOST ss-* rw-*
  USER rootadmin

HOST raritan
  HOSTNAME raritan.chass.utoronto.ca
  USER dominion

HOST gdr.mine.nu
  USER bdwalton

HOST netfirms
  HostName ssh.netfirms.com
  User psych1

HOST bar.utoronto.ca
  PORT 34

HOST tramp*
	User root

HOST bender
     HostName www.opencsw.org

HOST rw-103
        User rootadmin
        LocalForward 5901 localhost:5900
        ProxyCommand ssh -q -A -X login.cquest.utoronto.ca 'nc %h %p'

HOST ntdws13
        User rootadmin
        ForwardAgent yes
        ForwardX11 yes
        LocalForward 5901 localhost:5900
        #ProxyCommand ssh -q -A -X login.cquest.utoronto.ca 'nc -w1 %h %p'
        ProxyCommand ssh -q -A -X login.cquest.utoronto.ca 'nc %h %p'

HOST es-104
        ForwardAgent yes
        ForwardX11 yes
        LocalForward 5901 localhost:5900
        #ProxyCommand ssh -q -A -X login.cquest.utoronto.ca 'nc -w1 %h %p'
        ProxyCommand ssh -q -A -X login.cquest.utoronto.ca 'nc %h %p'

HOST *chass*
        ForwardX11 yes

